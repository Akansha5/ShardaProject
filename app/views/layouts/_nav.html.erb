<!-- we will render seperate nav bars depending on if the user is signed in or not. -->
<% if !user_signed_in? %>
  <%= render '/layouts/nav_guest' %> <!-- render to guest navigation bar -->
<% else %>
  <%= render '/layouts/nav_user' %> <!-- render to user navigation bar -->
<% end %>
